[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.28"
optimizer = true
optimizer_runs = 200

[fuzz]
# 한도 우회 시나리오를 자동 탐색하기 위해 기본 fuzz 횟수를 올린다 (T9)
runs = 512
